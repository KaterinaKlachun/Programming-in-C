# Занятие по C — 2026-04-16(2)

## Тема
1. Функции в языке C
2. Передача параметров в функции
3. Вычисление периметра и площади
4. Проверка корректности данных (неравенство треугольника)
5. Разделение программы на модули

## Цель
Научиться создавать функции, передавать параметры и разбивать программу на несколько файлов

## Задание
Написать функции, вычисляющие периметр и площадь треугольника по значениям длин трёх его сторон.
Разместить их в отдельном модуле.
Ввести с консоли значения сторон треугольника с проверкой корректности.
Вывести периметр и площадь.
---

## Тайминг (1.5 часа)
- 00:00–00:30 — разбор теории (функции, модули)
- 00:30–01:10 — практика (реализация функций)
- 01:10–01:30 — фиксация результата, коммиты, выводы

---

## Сделано
1. [ ] Разобраны функции
2. [ ] Реализован ввод данных
3. [ ] Добавлена проверка треугольника
4. [ ] Реализованы функции периметра и площади
5. [ ] Программа разбита на модули


---

## Коммиты / изменения
- commit: commit: разбор теории (функции)
- commit: commit: лабораторная работа №4 (треугольник)

---
## 1. Что такое функция

Функция — это блок кода, который выполняет определённую задачу и может возвращать значение.

Общий вид:
```
тип имя(параметры) {
// код
return значение;
}
```
---

## 2. Формулы

Периметр:
```
P = a + b + c
```
Площадь (формула Герона):
```
p = (a + b + c) / 2
S = √(p * (p - a) * (p - b) * (p - c))
```
---
## 3. Проверка треугольника

Треугольник существует, если:
```c
a + b > c
a + c > b
b + c > a
```
---

## 4. Структура проекта

Программа состоит из 3 файлов:
```c
main.c
triangle.c
triangle.h
```
---

## 5. Файл triangle.h

```c
   #ifndef TRIANGLE_H
   #define TRIANGLE_H

double perimeter(double a, double b, double c);
double area(double a, double b, double c);

#endif
```
---

## 6. Файл triangle.c
```c
   #include <math.h>
   #include "triangle.h"

double perimeter(double a, double b, double c) {
return a + b + c;
}

double area(double a, double b, double c) {
double p = perimeter(a, b, c) / 2;
return sqrt(p * (p - a) * (p - b) * (p - c));
}
```
---

## 7. Файл main.c
```c
   #include <stdio.h>
   #include <windows.h>
   #include "triangle.h"

int main() {
SetConsoleOutputCP(CP_UTF8);
SetConsoleCP(CP_UTF8);

    double a, b, c;

    printf("Введите стороны треугольника:\n");
    scanf("%lf %lf %lf", &a, &b, &c);

    if (a + b > c && a + c > b && b + c > a) {
        printf("Периметр: %.2lf\n", perimeter(a, b, c));
        printf("Площадь: %.2lf\n", area(a, b, c));
    } else {
        printf("Треугольник не существует\n");
    }

    return 0;
}
```
---

## 8. Итоговый алгоритм
1. Ввести стороны a, b, c
2. Проверить существование треугольника
3. Если существует:
    - вызвать функцию периметра
    - вызвать функцию площади
4. Вывести результат