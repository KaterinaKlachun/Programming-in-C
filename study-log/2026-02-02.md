# Занятие по C — 2026-02-02

**Тема:
1. что такое #include
2. что такое main
3. тип данных double
4. как объявлять переменные
5. что такое if / else
6. что такое printf
7. как считается дискриминант

**Цель: Изучить основы С**

## Тайминг (1.5 часа)
- 00:00–01:00 — разбор теории
- 1:00–01:20 — разбор на практике
- 01:20–01:30 — фиксация результата + коммиты + выводы

## Сделано
1. [ ] разбор теории
2. [ ] разбор на практике
3. [ ] фиксация результата + коммиты + выводы

## Коммиты/изменения
- commit: разбор теории
- commit: разбор на практике (лабораторная 1) 

## 1. Что такое #include
## Любая программа на C **обязательно** имеет следующую структуру:

```c
#include <stdio.h>

int main() {
    // Здесь пишется ваш код
    return 0;
}
```
**#include <stdio.h> — "Включаем инструменты"**

Что делает?
- Подключает библиотеку функций для ввода и вывода данных

📦 Что такое "библиотека"? - Это готовый набор функций, которые кто-то уже написал за вас

**stdio.h = STanDard Input/Output

Без этой строки вы не сможете:

- Выводить текст на экран (printf)

- Считывать ввод с клавивиатуры (scanf)

# 2. int main() — "Главная дверь программы"

Что делает?
- Это точка входа в программу. С этого места начинает выполняться код

🎯 Почему именно main?
Операционная система ищет функцию с именем main

Находит → заходит внутрь → выполняет код

Если main нет → программа не запустится

📐 Что означают слова?
int = integer (целое число) — функция ВОЗВРАЩАЕТ число

main = главный (обязательное название!)

() = скобки для аргументов (пока пустые)

**{ и } — "Стены комнаты"**

Что делает?
- Определяют границы функции
- Всё между { и } — это "тело" функции main

Визуализация:
```c
int main()
{                          // ← Дверь открылась
// Комната
// Весь ваш код здесь
// как мебель в комнате
}                          // ← Дверь закрылась
```

📌 Правило: Каждая открывающая { должна иметь закрывающую }

**// Здесь пишется ваш код — "Комментарий"**

Что делает?
- Игнорируется компьютером
- Нужен только людям для пояснений

💬 Виды комментариев:
```c
// Однострочный комментарий — всё после // игнорируется

/*
Многострочный
комментарий
между этими символами
*/
```

Зачем нужны?
- Объясняют сложные моменты
- Отключают код без удаления
- Делают программу понятнее

**return 0; — "Отчёт об успешном завершении"**

Что делает?
- Сообщает системе: "Программа завершилась УСПЕШНО!"

Коды возврата:
- return 0; → всё ок, ошибок нет
- return 1; (или любое не 0) → была ошибка
- Операционная система смотрит на это число

🎯 ВАЖНОЕ ПРАВИЛО:
Без этой структуры программа на C НЕ СУЩЕСТВУЕТ для компилятора

❌ Что будет если убрать...

| Убрали                   | Что произойдёт?                                         | 
|--------------------------|---------------------------------------------------------|
| ```#include <stdio.h>``` | Нельзя использовать ```printf```, ```scanf```           | 
| ```int main()	```        | **Ошибка компиляции:** "нет точки входа"                |
| ```{``` или ```}```      | **Ошибка синтаксиса**                                   |
| ```return 0;```          | Предупреждение, но может сработать                      |

*Пример РАБОЧЕЙ программы с пояснениями:*
```c
// Шаг 1: Берём инструменты для ввода-вывода
#include <stdio.h>

// Шаг 2: Объявляем главную дверь программы
int main() 
{
    // Шаг 3: Используем функцию printf из подключенной библиотеки
    // чтобы вывести текст на экран
    printf("Привет, мир!\n");  // \n - перенос на новую строку
    
    // Шаг 4: Сообщаем системе, что всё прошло успешно
    return 0;
    
    // Шаг 5: Закрываем "комнату" функции main
    // (скобка ниже)
}
// ↑ Эта фигурная скобка закрывает функцию main
```

Схема выполнения программы:
```text
[Запуск программы]
↓
[Система ищет main()]
↓
[Находит → переходит к первой строке в { }]
↓
[Выполняет ВСЕ инструкции сверху вниз]
↓
[Дошла до return 0; → завершение]
↓
[Система видит 0 → "Программа успешна!"]
```

# 3. Тип данных double
## double — «Числа с дробной частью»

Что делает?
- Хранит вещественные числа — то есть числа с точкой.

```c
double x = 3.14;
double y = -0.5;
double z = 10.0;
```

📌 Когда используется:
- при делении
- при работе с корнями
- в математических формулах

*❗ int — только целые
❗ double — целые + дробные*

# 4. Как объявлять переменные

Переменная — «Ячейка памяти с именем»

Общий формат:

```c
тип_данных имя_переменной;
```

Примеры:
```c
double a;
double b;
double c;
```

Сразу с заданием значения:

```c
double a = 1;
double b = -5;
double c = 6;
```

📌 Правила:
- имя не начинается с цифры
- пробелы запрещены
- a и A — разные переменные

# 5. Что такое if / else

## if / else — «Проверка условия»

Что делает?
- Выполняет код ТОЛЬКО если условие истинно.

Синтаксис:
```c
if (условие) {
// код
} else {
// код
}
```

Пример:
```c
if (D > 0) {
printf("Два корня\n");
} else {
printf("Корней нет\n");
}
```

📐 Операторы сравнения:
- '> больше'
- < меньше
- == равно
- != не равно

❌ Ошибка новичков:
```c
if (D = 0)   // ошибка
```

✅ Правильно:
```c
if (D == 0)
```

# 6. Что такое printf
## printf — «Вывод данных на экран»

Подключение:
```c
#include <stdio.h>
```

Простой вывод:
```c
printf("Hello, world!");
```

Вывод значения переменной:
```c
double x = 2.5;
printf("x = %lf\n", x);
```

📐 Форматные спецификаторы:
- %d — int
- %lf — double
- \n — переход на новую строку

Управление точностью:
```c
printf("x = %.2lf\n", x);
```

# 7. Как считается дискриминант

## Дискриминант — «Определяет количество корней»

Формула: `D = b² − 4ac`

Реализация в C:
```c
D = pow(b, 2) - 4 * a * c;
```

Подключение:
```c
#include <math.h>
```

Проверка значения:
```c++
if (D > 0) {
// два корня
} else if (D == 0) {
// один корень
} else {
// корней нет
}
```

Корни уравнения:
```c
x1 = (-b + sqrt(D)) / (2 * a);
x2 = (-b - sqrt(D)) / (2 * a);
```

Итоговая схема
```c++
[Задали a, b, c]
↓
[Вычислили D]
↓
[D > 0 ?]
├─ да → 2 корня
└─ нет
├─ D == 0 → 1 корень
└─ D < 0 → корней нет
```